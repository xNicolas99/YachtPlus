# Repository Knowledge Base (AGENTS.md)
> Auto-generated by Archivist Agent | Scan depth: multi-pass | Last updated: 2025-05-13T11:02:00Z
> Confidence legend: 🟢 HIGH | 🟡 MEDIUM | 🔴 LOW / Needs review

---

## 🏗️ Architecture & Stack

### Core Technology Stack
| Layer         | Technology         | Version  | Confidence |
|---------------|--------------------|----------|------------|
| Frontend      | Vue.js             | 3.4.0    | 🟢         |
| Backend       | FastAPI (Python)   | 0.111.0  | 🟢         |
| Database      | SQLite / SQLAlchemy| 2.0.45   | 🟢         |
| Build Tooling | Vite / pnpm        | 5.0.0    | 🟢         |

### Architecture Pattern
- **Primary Pattern:** Client-Server SPA (Vue 3 frontend, FastAPI REST backend).
- **Module Boundaries:** Backend organized by feature routers (`apps`, `templates`, `dashboard`, `auth`). Frontend separated into `views`, `components`, `store` (Vuex) / `stores` (Pinia), and `router`.
- **Monorepo / Polyrepo:** Monorepo. No specific tooling like Nx/Turborepo; standard dual-directory (`frontend/`, `backend/`) setup handled by Docker Compose.

### Data Flow (Traced End-to-End)
1. **Request Entry:** Client makes HTTP request to `/api/[route]` using Axios. Nginx proxies this to the backend `main.py` without trailing slash.
2. **Routing Layer:** FastAPI `app.include_router` directs requests to specific router modules (e.g., `api.routers.users`).
3. **Business Logic Layer:** Often inside routers or delegated to `api.actions` and `api.db.crud` helper modules. Setup process uses a DB state machine (`SetupStatus`).
4. **Data Access Layer:** SQLAlchemy ORM used with SQLite via `SessionLocal()`. Heavy reliance on Pydantic schemas for input validation and output serialization. Some state is pulled directly from the Docker daemon via `aiodocker` or `docker`.
5. **Response/Output:** JSON serialized through Pydantic models. Errors are raised as FastAPI `HTTPException`.

### External Integrations & APIs
| Service       | Purpose             | Integration Point          | Auth Method  |
|---------------|---------------------|----------------------------|--------------|
| Docker Daemon | Container Mgmt      | `/var/run/docker.sock`     | Local socket |
| Docker Hub    | Templates/Images    | `api.utils.registries`     | None/Public  |
| GHCR          | Image registries    | `api.utils.registries`     | None/Public  |

---

## 🗺️ Project Map (Annotated Directory Tree)

```
/
├── backend/                → [FastAPI application]
│   ├── alembic/            → [Database migrations]
│   ├── api/                → [Core application logic]
│   │   ├── actions/        → [Complex business logic (e.g., Docker actions)]
│   │   ├── auth/           → [JWT handling, security, and auth decorators]
│   │   ├── db/             → [Database connections, models, schemas, and CRUD]
│   │   │   ├── crud/       → [Database operation helpers]
│   │   │   ├── models/     → [SQLAlchemy ORM models]
│   │   │   └── schemas/    → [Pydantic schema validation]
│   │   ├── routers/        → [FastAPI route definitions]
│   │   └── utils/          → [Helper utilities (e.g., registries, security)]
│   ├── tests/              → [Pytest unit/integration tests]
│   └── main.py             → [Application entry point & middleware setup]
├── frontend/               → [Vue 3 SPA frontend]
│   ├── public/             → [Static assets]
│   ├── src/                → [Vue application source]
│   │   ├── components/     → [Reusable UI components]
│   │   ├── plugins/        → [Vue plugins (Vuetify, Utils)]
│   │   ├── router/         → [Vue Router configuration]
│   │   ├── store/          → [Legacy Vuex state management]
│   │   ├── stores/         → [Pinia state management]
│   │   └── views/          → [Top-level page components]
│   └── package.json        → [Frontend dependencies]
├── docs/                   → [Project documentation]
└── nginx.conf              → [Nginx reverse proxy configuration]
```

⚠️ Deviations from the above structure:
- `.jules/palette.md` → UX/UI learnings
- `.jules/sentinel.md` → Security findings
- `.jules/mechanic.md` → Logic/Testing
- `.jules/bolt.md` → Performance
- Several `.md` files in the root track security audits and debugging notes (e.g., `AUDIT_REPORT_EXECUTED.md`, `SECURITY_AUDIT_REPORT_2025.md`).

---

## 📜 Coding Conventions & Implicit Rules

### Naming Conventions
| Artifact | Convention | Example | Confidence |
|----------|------------|---------|------------|
| Vue Components | PascalCase | `GlobalSearch.vue` | 🟢 |
| API Routers | snake_case | `users.py` | 🟢 |
| FastAPI Paths | kebab-case / snake_case | `/auth/login_cookie` | 🟡 |
| DB Tables | snake_case | `user`, `apikey` | 🟢 |
| Python Functions | snake_case | `get_user_by_name()` | 🟢 |

### State Management
- **Pattern:** Both Pinia and Vuex are used concurrently for state management, as the architecture is in a transition state to Pinia.
- **Rule:** Avoid redundant network requests by using local state (e.g., `store.state.apps.apps`) when building search components or lists, unless querying external data.
- **Store location:** `frontend/src/store/` (Vuex), `frontend/src/stores/` (Pinia).
- 🔴 **Inconsistency found:** Both Vuex (`auth.js`) and Pinia (`authStore.js`) exist for authentication state. Vuex appears to be the primarily wired one in `router/index.js`, while components like `Login.vue` use Pinia's `authStore`.

### Data Fetching
- **Pattern:** Axios is the primary HTTP client.
- **Rule:** A global Axios interceptor handles 401 Unauthorized responses by attempting a token refresh.
- **Rule:** Independent API requests during initial loading must be executed concurrently using `Promise.all()` to minimize wait times.
- **Server-side:** Synchronous DB calls within async FastAPI endpoints must be wrapped in `fastapi.concurrency.run_in_threadpool` to avoid event-loop starvation. External requests should run concurrently with synchronous DB calls.

### Error Handling
- **Client errors:** Axios interceptors catch global errors. Components should catch local errors and trigger notifications via `this.notify()` or `$notify`.
- **Server errors:** Backend raises FastAPI `HTTPException`. Database updates violating unique constraints must catch exceptions, execute `db.rollback()`, and raise a 400 `HTTPException` to prevent unhandled 500 errors.

### Authentication & Authorization
- **Mechanism:** JWT-based authentication.
- **Session access pattern:**
  - Backend: Uses `Depends(get_auth_wrapper)` and `.jwt_required()`. Setup endpoints use `allow_setup_pending=True`.
  - Frontend: Vue Router `beforeEach` guard checks `store.state.auth.status` and `store.getters["auth/isAuthenticated"]`.
- **Security Check:** Endpoints verify user permissions (e.g., `perm_start`, `perm_restart`) against the database.
- **IP Restriction:** Login routes use `check_ip_restriction` to block non-private IPs by default.
- **Timing Attacks:** Password verification uses a unified path, testing against a real hash or a static `DUMMY_HASH` to prevent user enumeration.

### Styling
- **Primary system:** Vuetify 3 (Material Design).
- **Rule:** Dialog action buttons (`<v-card-actions>`) should consistently use the `text` attribute for 'Cancel' and semantic colors (e.g., `color="error"`) for destructive 'Continue' buttons.
- **Accessibility:** Icon-only buttons (`<v-btn icon>`) must include `aria-label` and `title`.

### TypeScript Rules
- ⚪ Not applicable (Frontend uses JavaScript).

---

## 🧪 Testing & Quality

### Test Infrastructure
| Category | Framework | Location | Coverage target |
|----------|-----------|----------|-----------------|
| Backend Unit/Integration | Pytest (`pytest-asyncio`) | `backend/tests/` | ⚪ Not found |
| Frontend Unit | Vitest | `frontend/tests/` (or adjacent) | ⚪ Not found |
| E2E / UI | Playwright | Scripts in root (`verify_*.py`) | ⚪ Not found |

### Key Commands
```bash
# Development (Backend)
cd backend && python main.py

# Development (Frontend)
cd frontend && pnpm dev

# Build (Frontend)
cd frontend && pnpm install && pnpm build

# Test (Backend)
cd backend && DATABASE_URL=sqlite:///:memory: PYTHONPATH=. python3 -m pytest tests/

# Test (Frontend)
cd frontend && pnpm vitest run

# UI Verification (Playwright)
cd frontend && pnpm run preview  # Starts preview server on 4173
```

### Mocking Strategy
- **Backend:** Heavy reliance on `unittest.mock` (e.g., `MagicMock`, `patch`) for complex dependencies.
- **Rule:** Docker-related tests must mock `subprocess.run` to prevent host execution. SSRF tests must mock `socket.getaddrinfo`.

---

## ⚙️ CI/CD & Deployment

### Pipeline Overview
| Workflow file | Trigger | Steps summary |
|---------------|---------|---------------|
| `.github/workflows/docker-image.yml` | Push/PR | Builds and pushes Docker images. |
| `.github/workflows/ghcr.yml` | Push | Builds and pushes Docker images to `ghcr.io`. |

### Deployment Target
- **Platform:** Docker container.
- **Build output:** The Vite build output (`frontend/dist/`) is served directly by the FastAPI backend via `StaticFiles`.
- **Proxy:** Nginx is used in production as a reverse proxy.

---

## ⚠️ Anomalies, Workarounds & Technical Debt

| ID | File / Area | Description | Risk | Action needed |
|----|-------------|-------------|------|---------------|
| W01 | `frontend/src/store/modules/auth.js` | Maps `email` to `username` inside `AUTH_REQUEST` to maintain compatibility with FastAPI's `OAuth2PasswordRequestForm`. | Low | Keep in mind when modifying login forms. |
| W02 | `backend/api/main.py` | Nginx reverse proxies `/api/` requests *without* a trailing slash to preserve routing. | Medium | Do not append trailing slashes to Nginx proxy passes. |
| W03 | `backend/api/db/crud/templates.py` | Uses a custom `SafeRedirectHandler` for SSRF protection via `urllib.request`. | High | Ensure any new URL fetching logic uses similar protections. |
| W04 | `backend/api/routers/setup/setup.py` | `is_setup_completed` checks DB state, but `access_token` issue sets `setup_pending: True` claim to restrict access until 2FA is done. | High | Modifying auth flows requires testing setup sequences. |
| W05 | `frontend/src/stores/authStore.js` and `frontend/src/store/modules/auth.js` | Concurrent usage of Vuex and Pinia. Auth logic is split between both, with `Login.vue` relying on Pinia and router guards relying on Vuex. | High | Standardize on one state management tool (ideally Pinia). |
| W06 | `frontend/vite.config.js` | Contains references to shims or compatibility layers for Vue 2 libraries in a Vue 3 app (e.g., vee-validate v3, vue-chartjs legacy). | High | Follow `AUDIT_REPORT_EXECUTED.md` roadmap for fixes. |

---

## 🆕 Dynamic Discoveries (New Patterns Found During This Scan)

| Discovery ID | Category | Description | Occurrences | Verdict |
|--------------|----------|-------------|-------------|---------|
| D01 | Inconsistency | Vue components directly import and use `axios` instead of relying entirely on Vuex/Pinia actions (e.g., `UserManagement.vue`, `ServerInfo.vue`). | 15+ | 🟡 Acceptable but breaks strict state-action separation. |
| D02 | Convention | Backend routing uses explicit `auth_check(Authorize)` calls inside each endpoint rather than global router dependencies. | 50+ | 🟢 Standardized pattern in this codebase. |
| D03 | Convention | Axios interceptor specifically intercepts 401 errors, queues a refresh token request, and replays failed requests. | 1 | 🟢 Core auth pattern. |
| D04 | Inconsistency | Login endpoint maps `email` parameter back to `username` to bridge legacy form assumptions with FastAPI backend schemas. | 1 | 🟢 Adopted as rule. |

---

## 📌 Agent Instructions for Future Tasks

- **Before creating a file:** Check the Project Map. Place backend routers in `api/routers`, frontend views in `src/views`.
- **Security First:** For URL validation, always catch `socket.gaierror` to avoid SSRF bypasses on unresolvable hosts. Validate client IPs right-to-left if using `X-Forwarded-For`.
- **Performance:** Do not block the async event loop in FastAPI; wrap synchronous DB calls in `run_in_threadpool`. Use `Promise.all()` in Vue for independent fetches.
- **Verification:** After modifying code, run `pytest` for backend and `vitest` for frontend. Do not commit `.coverage` files.
- **UI Consistency:** Use Vuetify 3 components, semantic colors, and ensure accessibility attributes (`aria-label`, `title`) are present on icon buttons.
- **Journaling:** Log UX learnings in `.jules/palette.md`, Security in `.jules/sentinel.md`, Testing/Logic in `.jules/mechanic.md`, and Performance in `.jules/bolt.md`.
